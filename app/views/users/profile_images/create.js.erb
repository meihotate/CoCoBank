var imageBox = document.getElementById('profile_image_box');
var image = document.getElementById('profile_image');
// imageBox.removeChild(image);

// var newImage = document.createElement('image');
var imageSrc = '<%= @image_src %>'
console.log(imageSrc);
image.src = imageSrc;
// imageBox.appendChild(newImage);

if (imageSrc === '/assets/no_image.jpg') {
	$('#flashnotice').html("プロフィール画像を選択してください。");
}else{
	var form = document.getElementById('imageform');
	form.innerHTML="";
	$('#flashnotice').html("プロフィール画像を保存しました。");
	$("#imageform").html("<%= escape_javascript(render '/users/users/update_image', user: @user, profile_image: @profile_image) %>");
};