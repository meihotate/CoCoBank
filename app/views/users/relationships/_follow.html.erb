<%= form_for current_user.active_relationships.build, url: users_following_path, remote: true do |f| %>
  <div><%= hidden_field_tag :followed_id, @user.id %></div>
  <%= f.submit "お気に入り登録する", class: "btn btn-primary" %>
<% end %>