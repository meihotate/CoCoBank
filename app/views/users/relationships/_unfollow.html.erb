<%= form_for current_user.active_relationships.find_by(followed_id: @user.id), url: users_followed_path, html: { method: :delete }, remote: true do |f| %>
  <div><%= hidden_field_tag :followed_id, @user.id %></div>
  <%= f.submit "お気に入り解除する", class: "btn" %>
<% end %>