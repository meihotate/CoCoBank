<%= form_for current_user.passive_friendships.find_by(from_user_id: @user.id), url: users_second_update_path(id: current_user.id, user_id: @user.id), remote: true do |f| %>
  <div><%= hidden_field_tag :from_user_id, @user.id %></div>
  <%= f.submit "やっぱり友達承認する", class: "btn btn-primary", id:"reapproveFriend" %>
<% end %>